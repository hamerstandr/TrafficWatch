<UserControl x:Class="TrafficWatch.View.Dashboard.DashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:local="clr-namespace:TrafficWatch.View.Dashboard"
        mc:Ignorable="d" 
        d:DesignHeight="450" d:DesignWidth="800"
        Background="{DynamicResource WindowBackground}">
    <Grid>
        <TabControl x:Name="AddonTabControl" 
                    Margin="10,10,10,10"
                    Background="Transparent"
                    BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" 
                                        BorderThickness="1,1,1,0" 
                                        CornerRadius="3,3,0,0" 
                                        Margin="2,0,2,0">
                                    <ContentPresenter x:Name="ContentSite" 
                                                      VerticalAlignment="Center" 
                                                      HorizontalAlignment="Center" 
                                                      ContentSource="Header" 
                                                      Margin="12,4"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Panel.ZIndex" Value="100" />
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource WindowBackground}" />
                                        <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource WindowForeground}" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="Transparent" />
                                        <Setter TargetName="Border" Property="BorderBrush" Value="Transparent" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            
            <!-- تب پیش‌فرض - خوش‌آمدگویی -->
            <TabItem Header="Dashboard">
                <Grid Margin="10">
                    <TextBlock Text="Welcome to TrafficWatch Dashboard! Select an addon tab to view details." 
                               FontSize="16" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource WindowForeground}"/>
                </Grid>
            </TabItem>
            
            <!-- تب‌های افزونه‌ها به صورت داینامیک اضافه می‌شوند -->
        </TabControl>
        
        <!-- پیام وقتی هیچ افزونه‌ای فعال نیست -->
        <Grid x:Name="NoAddonsGrid" Visibility="Collapsed">
            <TextBlock Text="No addons enabled. Please enable addons from Settings." 
                       FontSize="16" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource WindowForeground}"/>
        </Grid>
    </Grid>
</UserControl>
