<UserControl x:Class="TrafficWatch.View.Dashboard.SystemMonitorTab"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d" 
        d:DesignHeight="350" d:DesignWidth="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="System Monitor" 
                   FontSize="20" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Foreground="{DynamicResource WindowForeground}"
                   Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Top">
            <!-- CPU -->
            <Border BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="CPU Usage:" FontWeight="Bold" 
                               Foreground="{DynamicResource WindowForeground}"/>
                    <TextBlock Grid.Column="1" x:Name="TxtCPU" Text="Loading..." 
                               Foreground="{DynamicResource WindowForeground}"/>
                </Grid>
            </Border>

            <!-- RAM -->
            <Border BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="RAM Usage:" FontWeight="Bold" 
                               Foreground="{DynamicResource WindowForeground}"/>
                    <TextBlock Grid.Column="1" x:Name="TxtRAM" Text="Loading..." 
                               Foreground="{DynamicResource WindowForeground}"/>
                </Grid>
            </Border>

            <!-- Disk -->
            <Border BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Disk Usage:" FontWeight="Bold" 
                               Foreground="{DynamicResource WindowForeground}"/>
                    <TextBlock Grid.Column="1" x:Name="TxtDisk" Text="Loading..." 
                               Foreground="{DynamicResource WindowForeground}"/>
                </Grid>
            </Border>

            <!-- Network -->
            <Border BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Network:" FontWeight="Bold" 
                               Foreground="{DynamicResource WindowForeground}"/>
                    <TextBlock Grid.Column="1" x:Name="TxtNetwork" Text="Loading..." 
                               Foreground="{DynamicResource WindowForeground}"/>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
